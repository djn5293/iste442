VPATH = src include /usr/local/opt/flex/lib
CPPFLAGS = -I include
LEX=flex

countWords : countWords.o lexer.o
	gcc $^ -lfl -L/usr/local/opt/flex/lib -o $@

countWords.o : countWords.c
	gcc -c $<

lexer.o : lexer.c
	gcc -c $<

lexer.c : lexer.l
	flex -t $< > $@

clean :
	rm *.o lexer.c
